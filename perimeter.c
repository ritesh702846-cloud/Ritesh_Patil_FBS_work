#include <stdio.h>
int main()
{ 
int length,width,perimeter;

printf("Enter length: ");
scanf("%d", &length);

printf("enter width ");
scanf("%d", &length);

 perimeter = 2 * (length + width);
 printf("perimeter = %d",perimeter);
 return 0;

}